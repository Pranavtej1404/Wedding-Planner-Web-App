services:
  frontend:
    build: .
    container_name: wp-frontend
    ports:
      - "3000:3000"
    environment:
      # These would point to the backend server's IP/DNS in a multi-server setup
      - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL:-http://localhost:8080}
